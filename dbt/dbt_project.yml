name: 'job_market'
version: '1.0.0'
config-version: 2

profile: 'job_market'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  job_market:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
