name: 'job_market'
version: '1.0.0'
config-version: 2

profile: 'job_market'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

vars:
  # usd -> gbp conversion rate for the uk vs global salary comparison
  usd_to_gbp: 0.79

models:
  job_market:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
