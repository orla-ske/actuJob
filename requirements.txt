# Python packages installed into the Airflow containers.
# Add new dependencies here — they are picked up on the next `docker compose build`.

apache-airflow-providers-amazon==8.22.0   # S3Hook, S3 operators
dbt-duckdb==1.8.1                         # DBT adapter for DuckDB
duckdb==0.10.3                            # DuckDB engine
boto3==1.34.0                             # AWS SDK (LocalStack S3)
requests==2.31.0                          # HTTP calls to Adzuna / BLS APIs
elasticsearch==8.13.0                     # Elasticsearch Python client
pandas==2.2.2                             # DataFrame utilities
